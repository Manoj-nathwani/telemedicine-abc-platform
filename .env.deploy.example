# Google Cloud Platform deployment configuration
# Copy this to .env.deploy and fill in your values

PROJECT_ID=your-gcp-project-id
REGION=africa-south1
ARTIFACT_REGISTRY=docker
CLOUDSQL_DB=your-gcp-project-id:region:database-instance-name
