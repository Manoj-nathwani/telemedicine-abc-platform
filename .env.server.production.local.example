# Production database connection for LOCAL seeding only
# Copy this to .env.server.production.local and fill in your values
# Uses public IP to connect to Cloud SQL from your local machine
# Make sure your IP is whitelisted in Cloud SQL authorized networks

DATABASE_URL=postgresql://user:password@public-ip-address:5432/dbname
